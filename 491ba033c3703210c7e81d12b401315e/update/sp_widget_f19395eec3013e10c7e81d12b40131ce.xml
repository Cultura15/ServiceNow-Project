<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_widget">
    <sp_widget action="INSERT_OR_UPDATE">
        <category>custom</category>
        <client_script><![CDATA[api.controller = function($scope, $window, spUtil, $log) {
  var c = this;

  c.cartItems = c.data.cartItems || [];
  c.totalItems = c.data.totalItems || 0;
  c.grandTotal = c.data.grandTotal || 0;

  // Remove item
  c.removeFromCart = function(sys_id) {
    if (!confirm("Remove this item from your cart?")) return;

    c.data.action = 'remove_item';
    c.data.sys_id = sys_id;

    c.server.update(c.data).then(function(response) {
      if (response.success) {
        spUtil.addInfoMessage("]]>🗑<![CDATA[ Item removed!");
        c.cartItems = response.cartItems;
        c.totalItems = response.totalItems;
        c.grandTotal = response.grandTotal;
      } else {
        spUtil.addErrorMessage("❌ Failed to remove item: " + (response.error || "Unknown error"));
      }
    });
  };

  // Checkout
  c.checkout = function() {
    if (!c.cartItems || c.cartItems.length === 0) {
      spUtil.addErrorMessage("Cart is empty!");
      return;
    }

    if (!confirm("Proceed to checkout?")) return;

    c.data.action = 'checkout';
    c.data.cartItems = angular.copy(c.cartItems);

    $log.info("]]>🧾<![CDATA[ Sending checkout payload:", c.data);

    c.server.update(c.data).then(function(response) {
      if (response.success) {
        spUtil.addInfoMessage(response.message || "✅ Checkout successful!");
        c.cartItems = [];
        c.totalItems = 0;
        c.grandTotal = 0;

        setTimeout(function() {
          $window.location.href = '/psmp?id=psmp_my_orders';
        }, 1500);
      } else {
        spUtil.addErrorMessage("❌ Checkout failed: " + (response.error || "Please try again."));
      }
    }).catch(function(error) {
      $log.error("❌ Checkout error:", error);
      spUtil.addErrorMessage("An unexpected error occurred during checkout.");
    });
  };

  c.goBack = function() {
    $window.location.href = '/psmp?id=psmp_shop';
  };
};

api.controller.$inject = ['$scope', '$window', 'spUtil', '$log'];
]]></client_script>
        <controller_as>c</controller_as>
        <css>.cart-widget-container {&#13;
  padding: 20px;&#13;
  max-width: 800px;&#13;
  margin: 0 auto;&#13;
}&#13;
&#13;
.cart-title {&#13;
  font-size: 1.6rem;&#13;
  font-weight: 600;&#13;
  margin-bottom: 20px;&#13;
}&#13;
&#13;
.cart-items {&#13;
  display: flex;&#13;
  flex-direction: column;&#13;
  gap: 16px;&#13;
}&#13;
&#13;
.cart-item {&#13;
  display: flex;&#13;
  align-items: center;&#13;
  background: #fff;&#13;
  border-radius: 12px;&#13;
  box-shadow: 0 2px 8px rgba(0,0,0,0.08);&#13;
  padding: 12px;&#13;
  transition: transform 0.2s;&#13;
}&#13;
&#13;
.cart-item:hover {&#13;
  transform: scale(1.01);&#13;
}&#13;
&#13;
.cart-item-img {&#13;
  width: 90px;&#13;
  height: 90px;&#13;
  object-fit: cover;&#13;
  border-radius: 10px;&#13;
  margin-right: 15px;&#13;
}&#13;
&#13;
.cart-item-info {&#13;
  flex: 1;&#13;
}&#13;
&#13;
.cart-item-name {&#13;
  font-size: 1.1rem;&#13;
  font-weight: 600;&#13;
  margin: 0;&#13;
}&#13;
&#13;
.cart-item-brand {&#13;
  color: #888;&#13;
  margin: 4px 0;&#13;
}&#13;
&#13;
.cart-item-price,&#13;
.cart-item-total {&#13;
  font-size: 0.95rem;&#13;
  margin: 2px 0;&#13;
}&#13;
&#13;
.remove-btn {&#13;
  background: #ff5252;&#13;
  color: white;&#13;
  border: none;&#13;
  border-radius: 6px;&#13;
  padding: 6px 10px;&#13;
  cursor: pointer;&#13;
  transition: background 0.2s;&#13;
}&#13;
&#13;
.remove-btn:hover {&#13;
  background: #ff0000;&#13;
}&#13;
&#13;
.cart-summary {&#13;
  margin-top: 25px;&#13;
  background: #f9f9f9;&#13;
  border-radius: 10px;&#13;
  padding: 15px;&#13;
  text-align: right;&#13;
}&#13;
&#13;
.checkout-btn {&#13;
  background: #28a745;&#13;
  color: white;&#13;
  border: none;&#13;
  border-radius: 8px;&#13;
  padding: 10px 16px;&#13;
  margin-top: 10px;&#13;
  font-size: 1rem;&#13;
  cursor: pointer;&#13;
  transition: background 0.2s;&#13;
}&#13;
&#13;
.checkout-btn:hover {&#13;
  background: #218838;&#13;
}&#13;
&#13;
.empty-cart {&#13;
  text-align: center;&#13;
  padding: 40px;&#13;
  font-size: 1.2rem;&#13;
}&#13;
&#13;
.back-btn {&#13;
  background: #007bff;&#13;
  color: white;&#13;
  border: none;&#13;
  border-radius: 8px;&#13;
  padding: 10px 16px;&#13;
  cursor: pointer;&#13;
}&#13;
&#13;
.back-btn:hover {&#13;
  background: #0069d9;&#13;
}&#13;
</css>
        <data_table>sp_instance</data_table>
        <demo_data/>
        <description/>
        <docs/>
        <field_list/>
        <has_preview>false</has_preview>
        <id/>
        <internal>false</internal>
        <link><![CDATA[function link(scope, element, attrs, controller) {
  
}]]></link>
        <name>Pet Shop Cart</name>
        <option_schema/>
        <public>false</public>
        <roles>x_1607167_pet_shop.Pet Shop User</roles>
        <script><![CDATA[(function() {
  data.cartItems = [];
  data.totalItems = 0;
  data.grandTotal = 0;

  var currentUser = gs.getUserID();

  // ---------------------------------------------
  // Load active cart items
  // ---------------------------------------------
  var cart = new GlideRecord("x_1607167_pet_shop_pet_shop_cart");
  cart.addQuery("user", currentUser);
  cart.addQuery("active", true);
  cart.addQuery("ordered", false);
  cart.query();

  while (cart.next()) {
    // Initialize order_linked if undefined
    if (cart.order_linked === undefined) {
      cart.order_linked = false;
      cart.update();
    }

    data.cartItems.push({
      sys_id: cart.getUniqueValue(),
      item: cart.item.getDisplayValue(),
      brand: cart.getValue("brand"),
      category: cart.getValue("category"),
      price: parseFloat(cart.getValue("price") || 0),
      quantity: parseInt(cart.getValue("quantity") || 0, 10),
      total_price: parseFloat(cart.getValue("total_price") || 0),
      image: cart.getValue("image")
    });
  }

  data.totalItems = data.cartItems.length;
  data.grandTotal = data.cartItems.reduce(function(sum, item) {
    return sum + item.total_price;
  }, 0);

  // ---------------------------------------------
  // Remove item
  // ---------------------------------------------
  if (input && input.action === "remove_item") {
    var rm = new GlideRecord("x_1607167_pet_shop_pet_shop_cart");
    if (rm.get(input.sys_id)) {
      rm.active = false;
      rm.update();
    }

    // Reload remaining cart
    var reload = new GlideRecord("x_1607167_pet_shop_pet_shop_cart");
    reload.addQuery("user", currentUser);
    reload.addQuery("active", true);
    reload.addQuery("ordered", false);
    reload.query();

    data.cartItems = [];
    while (reload.next()) {
      data.cartItems.push({
        sys_id: reload.getUniqueValue(),
        item: reload.item.getDisplayValue(),
        brand: reload.getValue("brand"),
        category: reload.getValue("category"),
        price: parseFloat(reload.getValue("price") || 0),
        quantity: parseInt(reload.getValue("quantity") || 0, 10),
        total_price: parseFloat(reload.getValue("total_price") || 0),
        image: reload.getValue("image")
      });
    }

    data.totalItems = data.cartItems.length;
    data.grandTotal = data.cartItems.reduce(function(sum, item) {
      return sum + item.total_price;
    }, 0);

    data.success = true;
    return;
  }

  // ---------------------------------------------
  // Checkout: Create ONE order per user checkout + order details
  // ---------------------------------------------
  if (input && input.action === "checkout") {
    if (!data.cartItems || data.cartItems.length === 0) {
      data.success = false;
      data.error = "Cart is empty!";
      return;
    }

    var order = new GlideRecord("x_1607167_pet_shop_pet_shop_order");
    if (!order.isValid()) {
      data.success = false;
      data.error = "Order table invalid";
      return;
    }

    // ]]>🆕<![CDATA[ Create one parent order
    order.initialize();
    order.order_for = currentUser;
    order.state = "1"; // Pending
    order.short_description = "Order with " + data.totalItems + " item(s)";
    order.total_amount = data.grandTotal;

    var orderSysId = order.insert();
    gs.info("✅ Created single parent order: " + orderSysId);

    // ---------------------------------------------
    // Mark cart items as ordered and link to this order
    // ---------------------------------------------
    var markCart = new GlideRecord("x_1607167_pet_shop_pet_shop_cart");
    markCart.addQuery("user", currentUser);
    markCart.addQuery("active", true);
    markCart.addQuery("ordered", false);
    markCart.query();

    var createdDetails = 0;

    while (markCart.next()) {
      markCart.ordered = true;
      markCart.active = false;
      markCart.order_linked = true;      // mark as linked
      markCart.parent_order = orderSysId; // link to this order
      markCart.update();

      // ---------------------------------------------
      // Create corresponding order detail record
      // ---------------------------------------------
      var detail = new GlideRecord("x_1607167_pet_shop_pet_shop_order_details");
      detail.initialize();
      detail.pet_shop_order = orderSysId;           // reference to parent order
      detail.item_name = markCart.item.getDisplayValue(); // string name
      detail.brand = markCart.getValue("brand");
      detail.category = markCart.getValue("category");
      detail.quantity = markCart.getValue("quantity");
      detail.price = markCart.getValue("price");
      detail.image = markCart.getValue("image");
      detail.insert();

      createdDetails++;
    }

    gs.info("✅ Created " + createdDetails + " order details for order: " + order.getDisplayValue("number"));

    data.success = true;
    data.message =
      "✅ Checkout complete! Your order #" +
      order.getDisplayValue("number") +
      " has been created with " + createdDetails + " items.";

    // Clear cart display after checkout
    data.cartItems = [];
    data.totalItems = 0;
    data.grandTotal = 0;

    return;
  }
})();
]]></script>
        <servicenow>false</servicenow>
        <sys_class_name>sp_widget</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2025-11-09 10:36:23</sys_created_on>
        <sys_id>f19395eec3013e10c7e81d12b40131ce</sys_id>
        <sys_mod_count>13</sys_mod_count>
        <sys_name>Pet Shop Cart</sys_name>
        <sys_package display_value="Pet Shop Needs Management" source="x_1607167_pet_shop">491ba033c3703210c7e81d12b401315e</sys_package>
        <sys_policy/>
        <sys_scope display_value="Pet Shop Needs Management">491ba033c3703210c7e81d12b401315e</sys_scope>
        <sys_update_name>sp_widget_f19395eec3013e10c7e81d12b40131ce</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2025-11-11 09:10:01</sys_updated_on>
        <template><![CDATA[<div class="cart-widget-container" ng-if="c.cartItems.length > 0">
  <h2 class="cart-title">]]>🛒<![CDATA[ Your Shopping Cart</h2>

  <div class="cart-items">
    <div class="cart-item" ng-repeat="item in c.cartItems track by item.sys_id">
      <img ng-if="item.image" ng-src="/sys_attachment.do?sys_id={{item.image}}" alt="{{item.item}}" class="cart-item-img"/>
      <div class="cart-item-info">
        <h3 class="cart-item-name">{{item.item}}</h3>
        <p class="cart-item-brand">{{item.brand}}</p>
        <p class="cart-item-category">Category: {{item.category}}</p>
        <p class="cart-item-price">₱{{item.price}} × {{item.quantity}}</p>
        <p class="cart-item-total">Total: <strong>₱{{item.total_price}}</strong></p>
      </div>
      <button class="remove-btn" ng-click="c.removeFromCart(item.sys_id)">]]>🗑<![CDATA[ Remove</button>
    </div>
  </div>

  <div class="cart-summary">
    <p><strong>Total Items:</strong> {{c.totalItems}}</p>
    <p><strong>Grand Total:</strong> ₱{{c.grandTotal}}</p>
    <button class="checkout-btn" ng-click="c.checkout()">✅ Checkout</button>
  </div>
</div>

<div class="empty-cart" ng-if="c.cartItems.length === 0">
  <p>Your cart is empty ]]>🐾<![CDATA[</p>
  <button class="back-btn" ng-click="c.goBack()">⬅ Back to Shop</button>
</div>
]]></template>
    </sp_widget>
    <sys_es_latest_script action="INSERT_OR_UPDATE">
        <id>f19395eec3013e10c7e81d12b40131ce</id>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2025-11-09 10:36:22</sys_created_on>
        <sys_id>952495eec3013e10c7e81d12b40131ba</sys_id>
        <sys_mod_count>0</sys_mod_count>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2025-11-09 10:36:22</sys_updated_on>
        <table>sp_widget</table>
        <use_es_latest>true</use_es_latest>
    </sys_es_latest_script>
</record_update>
